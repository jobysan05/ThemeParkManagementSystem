@model TempTPMS.Models.RIDES_STAFF

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>RIDES_STAFF</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.EndDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.EndDate)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.RIDE.RideName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.RIDE.RideName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.STAFF.EmployeeType)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.STAFF.EmployeeType)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
