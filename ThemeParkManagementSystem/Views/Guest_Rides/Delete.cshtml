@model ThemeParkManagementSystem.Models.GUEST_RIDES

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>GUEST_RIDES</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.GUEST.FirstName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.GUEST.FirstName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.RIDE.RideName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.RIDE.RideName)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
