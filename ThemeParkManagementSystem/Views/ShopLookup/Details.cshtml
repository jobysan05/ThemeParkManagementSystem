@model ThemeParkManagementSystem.Models.SHOPLOOKUP

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Shop Types</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            ID
        </dt>
        <dd>
            @Html.DisplayFor(model => model.ID)
        </dd>
        <dt>
            Shop Type
        </dt>
        <dd>
            @Html.DisplayFor(model => model.ShopType)
        </dd>
    </dl>
</div>
<p>
    @if (Convert.ToBoolean(ViewData["userRole"]))
    {
        @Html.ActionLink("Edit", "Edit", new { id = Model.ID }) <text> | </text>
    }
    @Html.ActionLink("Back to List", "Index")
</p>
